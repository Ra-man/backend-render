server:
  port: 8081
spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/bcprodutos
    password: ${DB_PASSWORD}
    username: ${DB_USER}
    driverClassName: org.postgresql.Driver
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
          auth: 'true'
    host: smtp.gmail.com
    username: roberto.fmedeiros62@gmail.com
    password: "bbul jtmq otqv evgd"
    port: '587'
